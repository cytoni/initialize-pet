const AnimalTitle = (props) => {
          return ( 
          <div className="animal_title">
            <h2 className="entry">{props.name}</h2>
          </div>
       );
  };
   
  export default AnimalTitle;